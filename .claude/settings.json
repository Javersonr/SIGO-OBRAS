{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "async": true,
            "statusMessage": "Auto-commit + push",
            "command": "cd /c/Users/javer/sigoobras-base && git add -A && (git diff --cached --quiet || (git commit -m 'chore: auto-commit após edição [skip ci]' -m 'Co-Authored-By: Claude <noreply@anthropic.com>' && git push origin master))"
          }
        ]
      }
    ]
  }
}
